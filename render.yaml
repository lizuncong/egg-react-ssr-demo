services:
  - type: web
    name: egg-react-ssr-demo
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build-react
    startCommand: npm run start:render
    envVars:
      - key: NODE_ENV
        value: production
